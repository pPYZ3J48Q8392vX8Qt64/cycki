{
  "name": "powerbanks",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "core-js": "^3.6.5",
    "nuxt": "^2.14.5",
    "push-dir": "^0.4.1"
  },
  "devDependencies": {}
}
